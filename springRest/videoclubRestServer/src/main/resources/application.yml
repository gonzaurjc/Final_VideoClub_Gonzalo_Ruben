server:
  port: 8080

spring:
    jpa:
        database: MYSQL
        hibernate:
            ddl-auto: validate

    datasource:
        url: jdbc:mysql://localhost/videoclub
        username: root
        password: aguilar
        driver-class-name: com.mysql.jdbc.Driver